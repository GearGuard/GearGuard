# GearGurd
2nd Year Group Project UCSC

# Docker
1. Start docker on your desktop
2. On terminal run
`docker compose up -d`

3. Docker Composer
`docker compose exec app composer dump-autoload`

4. Docker Autoloading
`docker compose exec app composer require symfony/var-dumper`